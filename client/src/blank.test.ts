// TODO - Remove this test. It's only here since there are currently no other client-side tests.
describe('Example test suite', () => {
  test('Example test', () => {
    expect(true).toEqual(true);
  });
});
